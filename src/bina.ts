/**
 *
 * @param array  It is expected to return a negative value if the first argument is less than the second argument, zero if they're equal
 * @returns sortedArray
 */

function binarySearch(array: (string | number)[]) {
  const sortedArray = array.sort();
  return sortedArray;
}

export default binarySearch;
