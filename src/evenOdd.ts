/**
 *
 * @param n input number check if even or odd.
 * @returns return from function either true or false.
 */

function evenOdd(n: number) {
  return n % 2 === 0 ? "true" : "false";
}

export { evenOdd };
