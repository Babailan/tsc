{
  "name": "ts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "rm -rf ./dist && tsc",
    "watch": "rm -rf ./dist && tsc -w"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
